{
  "name": "arena-mrv-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node ./Api/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "victorgois",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.11.25",
    "typescript": "^5.4.2"
  }
}